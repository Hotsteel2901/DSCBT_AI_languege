// struct.dscbt - 结构体示例

class Point:
    x: int
    y: int

p = Point { x: 10, y: 20 }
print(p.x)
print(p.y)

p.x = 100
print(p.x)

class Rect:
    tl: Point
    br: Point

r = Rect { tl: Point { x: 0, y: 0 }, br: Point { x: 10, y: 10 } }
print(r.tl.x)
print(r.br.y)
